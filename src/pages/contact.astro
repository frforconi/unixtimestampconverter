---
import Layout from '../layouts/Layout.astro';
---

<Layout 
	title="Contact Us | Support for Unix Timestamp Converter"
	description="Need help or found a bug? Reach out to the Unix Timestamp Converter team for support, feature requests, or technical feedback."
>
	<div class="content-container">
		<header class="page-header">
			<div class="badge">Feedback</div>
			<h1>Get in <span class="text-gradient">Touch</span></h1>
			<p class="subtitle">We value your input and are here to help with any technical inquiries.</p>
		</header>

		<section class="contact-grid">
			<div class="contact-info">
				<h2>How can we help?</h2>
				<p>
					Whether you've found a bug, have a suggestion for a new feature, or simply want to say hello, we're all ears. Our goal is to make this the best Unix time tool on the web.
				</p>
				
				<div class="contact-methods">
					<div class="method">
						<span class="icon">📧</span>
						<div class="method-details">
							<span class="label">Email Us</span>
							<a href="mailto:website@unixtimestampconverter.com" class="value">website@unixtimestampconverter.com</a>
						</div>
					</div>
					<div class="method">
						<span class="icon">🛠️</span>
						<div class="method-details">
							<span class="label">Support Hours</span>
							<span class="value">Mon-Fri, 9:00 - 18:00 UTC</span>
						</div>
					</div>
				</div>
			</div>

			<div class="contact-card">
				<div class="card-glow"></div>
				<h3>Quick Feedback</h3>
				<p>Found something that could be improved? Let us know!</p>
				<ul class="feedback-types">
					<li>✨ Suggest a new tool</li>
					<li>🐞 Report a calculation bug</li>
					<li>🎨 Improvement for UI/UX</li>
					<li>📈 Feature requests</li>
				</ul>
			</div>
		</section>

		<div class="back-link">
			<a href="/">&larr; Back to Home</a>
		</div>
	</div>
</Layout>

<style>
	.content-container {
		max-width: 1000px;
		margin: 2rem auto;
		background: var(--card-bg);
		backdrop-filter: blur(20px);
		padding: 4rem;
		border-radius: 32px;
		border: 1px solid var(--card-border);
		box-shadow: 0 20px 50px rgba(0,0,0,0.3);
	}

	.page-header {
		text-align: center;
		margin-bottom: 5rem;
	}

	.badge {
		display: inline-block;
		background: rgba(var(--accent-light), 0.1);
		color: rgb(var(--accent-light));
		padding: 0.5rem 1.2rem;
		border-radius: 100px;
		font-size: 0.75rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 1rem;
		border: 1px solid rgba(var(--accent-light), 0.2);
	}

	h1 {
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 900;
		margin: 0;
		line-height: 1.1;
		letter-spacing: -0.03em;
	}

	.text-gradient {
		background: linear-gradient(135deg, rgb(var(--accent)), rgb(var(--accent-light)));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.subtitle {
		color: #8b949e;
		font-size: 1.2rem;
		margin-top: 1rem;
	}

	.contact-grid {
		display: grid;
		grid-template-columns: 1.2fr 1fr;
		gap: 5rem;
		align-items: start;
	}

	.contact-info h2 {
		color: #fff;
		font-size: 2rem;
		margin-bottom: 1.5rem;
	}

	.contact-info p {
		color: #8b949e;
		line-height: 1.8;
		font-size: 1.1rem;
		margin-bottom: 3rem;
	}

	.contact-methods {
		display: flex;
		flex-direction: column;
		gap: 2.5rem;
	}

	.method {
		display: flex;
		gap: 1.5rem;
		align-items: center;
	}

	.icon {
		font-size: 2rem;
		background: rgba(255, 255, 255, 0.05);
		width: 60px;
		height: 60px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 16px;
		border: 1px solid var(--card-border);
	}

	.method-details {
		display: flex;
		flex-direction: column;
	}

	.label {
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: #8b949e;
		margin-bottom: 0.3rem;
	}

	.value {
		color: #fff;
		font-size: 1.1rem;
		font-weight: 600;
		text-decoration: none;
		transition: color 0.2s;
	}

	a.value:hover {
		color: rgb(var(--accent-light));
	}

	.contact-card {
		background: rgba(var(--accent-dark), 0.3);
		padding: 3rem;
		border-radius: 24px;
		border: 1px solid rgba(var(--accent-light), 0.1);
		position: relative;
		overflow: hidden;
	}

	.card-glow {
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle at center, rgba(var(--accent), 0.1) 0%, transparent 50%);
		pointer-events: none;
	}

	.contact-card h3 {
		color: #fff;
		font-size: 1.5rem;
		margin: 0 0 1rem 0;
	}

	.contact-card p {
		color: #8b949e;
		font-size: 0.95rem;
		margin-bottom: 2rem;
	}

	.feedback-types {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.feedback-types li {
		color: #c9d1d9;
		font-size: 0.95rem;
		padding: 0.8rem 1.2rem;
		background: rgba(255, 255, 255, 0.05);
		border-radius: 12px;
		border: 1px solid rgba(255, 255, 255, 0.05);
	}

	.back-link {
		margin-top: 5rem;
		border-top: 1px solid var(--card-border);
		padding-top: 2rem;
	}

	.back-link a {
		color: #8b949e;
		text-decoration: none;
		font-weight: 600;
		transition: color 0.2s;
	}

	.back-link a:hover {
		color: #fff;
	}

	@media (max-width: 1024px) {
		.contact-grid {
			grid-template-columns: 1fr;
			gap: 4rem;
		}
	}

	@media (max-width: 768px) {
		.content-container {
			padding: 2rem;
		}
	}
</style>
